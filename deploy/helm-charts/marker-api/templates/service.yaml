apiVersion: v1
kind: Service
metadata:
  name: {{ include "marker-api.fullname" . }}
  labels:
    {{- include "marker-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marker-api.selectorLabels" . | nindent 4 }}
