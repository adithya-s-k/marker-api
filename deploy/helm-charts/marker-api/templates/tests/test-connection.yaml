apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "marker-api.fullname" . }}-test-connection"
  labels:
    {{- include "marker-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "marker-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
